name: mimic
min_off_hours: 24.0
events_csv: ${paths.data_dir}/mimic/crrt_events.csv
labs_csv: ${paths.data_dir}/mimic/labs.csv
flags_csv: ${paths.data_dir}/mimic/flags.csv

# Per-modality off-threshold for attempt detection (IHD intermittent vs CRRT continuous).
# CRRT 24h; IHD 72h (> routine daily/alternate-day scheduling). Sensitivity: 48/72/96h.
min_off_hours_by_class:
  CRRT: 24.0
  IHD: 72.0
